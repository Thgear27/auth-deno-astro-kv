---
import Welcome from "../components/Welcome.astro";
import Layout from "../layouts/Layout.astro";
import { kv } from "../db/client";

// await kv.set(["test", 1], { name: "test", value: 1 });
// await kv.set(["test", 2], { name: "test", value: 2 });
// await kv.set(["test", 3], { name: "test", value: 3 });
// await kv.set(["test2", 4], { name: "test", value: 4 });
// await kv.set(["test2", 5], { name: "test", value: 5 });


// console.log("--------------- List ----------------");
// const secondList = kv.list({prefix: ["test"]});
// for await (const item of secondList) {
//   console.log(item);
// }
---

<Layout>
  <Welcome />
</Layout>
